import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
    customizeToolbar: {
        minHeight: 120,
        width: "100%",
        marginBottom: "50px"
    }
});

export { useStyles };